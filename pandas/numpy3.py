import numpy as np

# Generate a 3x3 matrix of random numbers between 0 and 1
random_matrix = np.random.rand(3, 3)
print(random_matrix)

# Generate a 3x3 matrix of random integers between 0 and 10
random_int_matrix = np.random.randint(0, 10, (3, 3))
print(random_int_matrix)